# SDD Configuration — travesiafiunmdp-main
# Generated by sdd-init on 2026-06-27

project:
  name: travesiafiunmdp-main
  description: "Travesía FIUNMDP — student scoring and intervention tracking platform"

stack:
  runtime: node
  framework: react
  language: typescript
  bundler: vite
  styling: tailwindcss + daisyui
  backend: supabase
  routing: react-router-dom v7
  animations: framer-motion
  charts: recharts
  path_alias: "@/*"

testing:
  framework: none
  runner: none
  strict_tdd: false
  command: null
  note: "No test runner detected. vitest is recommended for this Vite + React + TypeScript stack. Install with: npm install -D vitest @testing-library/react @testing-library/jest-dom jsdom"

conventions:
  module_system: esm
  jsx: react-jsx
  typescript_target: es2022
  path_alias: "@/*" maps to project root
  lint_command: "tsc --noEmit"
  build_command: "vite build"
  dev_command: "vite --port=3000 --host=0.0.0.0"

persistence:
  mode: hybrid
  openspec: true
  engram: true
  topic_key: "sdd-init/travesiafiunmdp-main"

supabase:
  mcp_remote: true
  mcp_local: true
  has_functions: true
  has_migrations: true
  has_seed: true
